# Code Helper Chat UI

Minimal chat page for the Crew API. **Serve:** `cd chat_ui && python3 -m http.server 3000` then open http://localhost:3000. **API base:** set `window.CODE_HELPER_API` before the script loads, or use query param `?api=http://localhost:8000` (default is `http://localhost:8000`).
