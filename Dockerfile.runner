# code-helper runner service (execute commands in project)
FROM python:3.13-slim

WORKDIR /app

COPY . .
RUN pip install --no-cache-dir -e .

EXPOSE 8080
CMD ["uvicorn", "runner.app:app", "--host", "0.0.0.0", "--port", "8080"]
